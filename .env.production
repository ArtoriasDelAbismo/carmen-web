VITE_BACKEND_URL=https://cuidarte-ia-backend-production.up.railway.app

# "openai" (default/fallback) or "elevenlabs" (accent-quality experiment).
# Override in Netlify's env var UI to switch without touching code — no
# redeploy-via-git-revert needed to go back, just flip this and redeploy.
VITE_VOICE_PROVIDER=openai
